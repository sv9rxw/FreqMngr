<Window x:Class="FreqMngr.MainWindow" 
        x:Name="WindowMain"                   
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:s="clr-namespace:System;assembly=mscorlib"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FreqMngr"                   
        xmlns:models="clr-namespace:FreqMngr.Models"        
        xmlns:ViewModels="clr-namespace:FreqMngr.ViewModels"        
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"        
        xmlns:xcdg="http://schemas.xceed.com/wpf/xaml/datagrid"               
        mc:Ignorable="d"
        Title="FreqMngr" Height="800" Width="1024">
    <Window.Resources>
        <ViewModels:MainWindowViewModel x:Key="MainWindowViewModelDataSource" d:IsDataSource="True"/>


    </Window.Resources>

    <DockPanel x:Name="PnlMain" DataContext="{Binding Source={StaticResource MainWindowViewModelDataSource}}" >
        <ToolBar x:Name="BarMain" DockPanel.Dock="Top"/>
        <Grid x:Name="GridMain" DockPanel.Dock="Bottom" Width="1020">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ClmGroups"/>
                <ColumnDefinition x:Name="ClmFreqs" Width="3*"/>
                <ColumnDefinition x:Name="ClmEdit" Width="2*"/>
            </Grid.ColumnDefinitions>

            <TreeView x:Name="TreeGroups" Grid.Column="0" ItemsSource="{Binding Groups}">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}" DataType="{x:Type models:Group}">
                        <TextBlock Text="{Binding Name}"/>
                    </HierarchicalDataTemplate>                    
                </TreeView.ItemTemplate>
            </TreeView>

            <xcdg:DataGridControl x:Name="DataGridFreqs" Grid.Column="1" ItemsSource="{Binding Freqs}">
                <xcdg:DataGridControl.View>
                    <xcdg:TableflowView FixedColumnCount="1" />
                </xcdg:DataGridControl.View>
            </xcdg:DataGridControl>



        </Grid>
    </DockPanel>
</Window>
